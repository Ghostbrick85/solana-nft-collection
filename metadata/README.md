# Metadata

Upload `assets/images` to IPFS, replace `REPLACE_IMAGE_CID` in all ten JSON files with the returned CID, and upload this `metadata` folder to IPFS. Set `BASE_TOKEN_URI=ipfs://<metadata-CID>/` before deployment. Keep the trailing slash.
